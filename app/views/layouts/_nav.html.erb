<nav class="navbar navbar-default">
  <div class="container-fluid">
    <header class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if user_signed_in? %>
        <%= link_to '<i class="glyphicon glyphicon-home"></i> Your FOODweek'.html_safe, dashboard_path, class: 'navbar-brand' %></li>
      <% else %>
        <a class="navbar-brand" href="/">FOODweek</a>
      <% end %>
    </header>

    <section class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
				<% if user_signed_in? %>
          <% if current_user.meal_plans.last.present? %>
            <li><%= link_to '<i class="glyphicon glyphicon-list-alt"></i> Grocery List'.html_safe, grocery_list_path, target: "_blank" %></li>
          <% end %>
					<li><%= link_to '<i class="glyphicon glyphicon-plus"></i> Add Recipe'.html_safe, new_recipe_path %></li>
					<li><%= link_to '<i class="glyphicon glyphicon-cog"></i> Settings'.html_safe, edit_user_registration_path, :method => :get %></li>
          <li><%= link_to "<i class='glyphicon glyphicon-log-out'></i> #{t('devise.nav.log_out')}".html_safe, destroy_user_session_path, :method => :delete %><li>
				<% else %>
          <li><%= link_to "<i class='glyphicon glyphicon-log-in'></i> #{t('devise.nav.log_in')}".html_safe, new_user_session_path, :method => :get %></li>
          <li><%= link_to "<i class='glyphicon glyphicon-user'></i> #{t('devise.nav.sign_up')}".html_safe, new_user_registration_path, :method => :get %></li>
				<% end %>
			</ul>
		</section>
	</div>
</nav>
